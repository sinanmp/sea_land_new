{
  "version": 2,
  "builds": [
    {
      "src": "index.js", // Adjust this to the entry file of your application
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.js" } // Route all requests to index.js
  ]
}
